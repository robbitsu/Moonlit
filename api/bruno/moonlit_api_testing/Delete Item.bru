meta {
  name: Delete Item
  type: http
  seq: 12
}

delete {
  url: {{base_url}}/inventory/{{player_id}}/{{item_name}}
  body: json
  auth: inherit
}

body:json {
  {
    "item_name": "Milk of Amnesia"
  }
}

vars:pre-request {
  item_name: Milk of Amnesia
}

settings {
  encodeUrl: true
}
